package com.helix.ui.neuralstrand

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp

/**
 * Design tokens locked by the HELIX Design System Specification v1.0.
 * Values are copied verbatim from that specification, not re-derived here —
 * if a token needs to change, it changes there first, then here.
 */
internal object HelixDesignTokens {
    val AccentEmber = Color(0xFFFFB36B)
    val ColorMuted = Color(0xFF5D6B78)
    val LineThickness = 2.dp
    val TouchTargetWidth = 48.dp
}
